# Serving-only dependencies (training extras like mlflow/matplotlib/spacy
# stay out of the API image; torch is installed separately as CPU-only)
pandas>=2.0,<3
scikit-learn>=1.3,<2
pyyaml>=6.0,<7
transformers>=4.40,<5
langdetect>=1.0.9
nltk>=3.8,<4
fastapi>=0.110,<1
uvicorn[standard]>=0.29
pydantic>=2.6,<3
